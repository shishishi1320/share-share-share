.work__share
- if user_signed_in?
  = link_to "新規投稿", new_work_path, class: "header__right--btn"
.work__title
  仕事・勉強
- @works.each do |work|
  .content
    .content__left
      .content__left--image
    .content__right
      .content__right__top
        = link_to work.title, work_path(work.id),class: "content__right__top--title"
      .content__right__bottom
        = work.user.nickname
        .content__right__bottom--date
          = work.created_at
